from setuptools import setup, find_packages

setup(
    name = "src",
    version = "0.0.1", 
    description= "its a wine 0 packages",
    author="sehjbir",
    packages = find_packages(),
    license = "MIT"
)